/**
 * authRoutes.js
 * TODO: Implement theo phan cong
 */
const router = require('express').Router();
const auth   = require('../middleware/authMiddleware');
const role   = require('../middleware/roleMiddleware');

/* TODO: them routes */
// router.get('/', auth, (req, res) => { ... });

module.exports = router;